# @openfly/template-doc

> openfly doc template

## Usage

```sh
$ npx @openfly/cli init

$ ? Pick template source › - Use arrow-keys. Return to submit.
$     Git Repository
$ ❯   NPM Package

$ ? Input template name › @openfly/template-doc
```
